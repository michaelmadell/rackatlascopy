export function findRoomsContainingPoint(rooms: any[], point: { x: number; y: number }) {
  return [];
}

export function isPointInPolygon(point: [number, number], vs: Array<[number, number]>) {
  return false;
}

export function transformCoordinatesForScale(coord: any, scale: number) {
  return coord;
}
